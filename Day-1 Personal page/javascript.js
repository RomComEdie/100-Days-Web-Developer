const button = document.getElementById("modeGelap")
const x = document.getElement;

const simpan = localStorage.getItem("modeGelap");

if (simpan) {
    x.dataset.theme = simpan
}

button.addEventListener("click", () => {
    
})